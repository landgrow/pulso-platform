# Memory Index — PULSO

- [Épico 0 — Fundação: Auth, RLS, Shell Next.js](epics/epic-0-foundation/EPIC.md) — Setup Next.js 15 + TypeScript + shadcn + Supabase Auth + RLS. Iniciado em 05/09/2026.
- [Story 0.1 — Setup Next.js 15](epics/epic-0-foundation/0.1.story.md) — Setup completo do projeto. Status: ✅ Concluída (05/09/2026). Gates: typecheck ✅, lint ✅, build ✅.
- [Story 0.2 — Supabase Auth](epics/epic-0-foundation/0.2.story.md) — Auth completo com Server Actions, forgot/reset password, rate limit, edge cases. Status: ✅ Concluída (05/09/2026). Gates: typecheck ✅, lint ✅, build ✅.
- [Story 0.3 — RLS default-deny](epics/epic-0-foundation/0.3.story.md) — 6 tabelas multi-tenant, políticas RLS, funções helper, triggers, audit log, migrations SQL. Status: ✅ Concluída (05/09/2026).
- [Story 0.4 — Shell da aplicação](epics/epic-0-foundation/0.4.story.md) — Layout de grupo autenticado, sidebar, header, user dropdown, responsive. Status: ✅ Concluída (05/09/2026). Gates verdes.
- [Épico 1 — Motor de Coleções](epics/epic-1-collections/EPIC.md) — Ingestão/normalização de dados do cliente (formulário, upload, texto, áudio, versionamento, visão consolidada, gates finais). Todas as 8 stories (47 pts) implementadas. Não sobrou trabalho de código — só 2 passos manuais de infra: aplicar migrations 0007/0008 num Postgres real (1.6) e criar um usuário de teste dedicado pra ativar os e2e reais (1.8, `docs/testing/e2e-setup.md`).
- [Story 1.3 — Formulário estruturado com auto-save](epics/epic-1-collections/1.3.story.md) — Retomada de sessão do Cursor que ficou sem créditos no meio da implementação (arquivos já existiam, tracking nunca foi atualizado, gates nunca tinham rodado). Corrigidos: 11 erros de typecheck (dois sistemas de toast coexistindo — shadcn morto vs. sonner real — foi a causa raiz da maioria), e 2 bugs reais só descobertos ao escrever os testes (`buildFormularioSchema()` quebrava sempre que chamada; campos numéricos opcionais não eram realmente opcionais). Status: ✅ Concluída (05/09/2026). Gates: typecheck ✅, lint ✅, build ✅, test ✅ (97/97), e2e ✅ (escopo do smoke test desta story). Achado à parte: 4 testes e2e pré-existentes do Épico 0 falham (nunca tinham rodado — browsers do Playwright não estavam instalados) — não é regressão desta story, ver task separada.
- [Story 1.4 — Upload de arquivos (PDF/Excel/CSV) com Storage](epics/epic-1-collections/1.4.story.md) — Construída do zero (nada existia no disco ainda). Achado que reduziu escopo: bucket + RLS de `storage.objects` já estavam prontos em `db/migrations/0006_storage_evidencias.sql`, não precisou de migração nova. Achado de segurança: `xlsx` do npm tem prototype-pollution de severidade alta sem correção no registro — trocado pela build oficial da SheetJS via CDN deles (fix recomendado pelo próprio projeto). Status: ✅ Concluída (05/09/2026). Gates: typecheck ✅, lint ✅, build ✅, test ✅ (128/128), e2e ✅ (mesmo escopo limitado da 1.3 — só confirma autenticação obrigatória).
- [Story 1.5 — Texto livre + Áudio com transcrição](epics/epic-1-collections/1.5.story.md) — Construída do zero. Achado crítico: a story original assumia um content block `type: "audio"` na Claude API que não existe (só `text`/`image`/`document`) — verificado via skill `claude-api` + WebFetch aos docs oficiais. Nayara decidiu explicitamente adiar o áudio até escolher um vendor de ASR real (Whisper/Deepgram/AssemblyAI/Google/AWS); só texto livre foi implementado. Bug pré-existente corrigido de tabela: `deleteColecao`/`discardColecao` em `colecoes.ts` usavam um alias de embed PostgREST inexistente (`periodo_dados` em vez de `periodos_dados`), quebrando silenciosamente desde a Story 1.3 (sem testes cobrindo essas duas funções) — só virou bloqueante agora porque 1.5 reaproveita `discardColecao` para excluir textos. Achado de segurança não corrigido aqui (task separada aberta): RLS de `colecoes` (insert/update) não exclui `client_viewer`, então um viewer consegue escrever direto no banco contornando a checagem de `canEdit` da app. Status: 🟡 Parcial (05/09/2026) — 3 de 5 pts (texto livre). Gates (escopo texto livre): typecheck ✅, lint ✅, build ✅, test ✅ (141/141), e2e ✅ (mesmo escopo limitado das stories anteriores).
- [Story 1.6 — Versionamento e rollback de coleções](epics/epic-1-collections/1.6.story.md) — Implementado via trigger `BEFORE UPDATE` (`trg_colecoes_archive`) em vez de reescrever `upsertColecao`/`updateTextoLivre` diretamente — qualquer UPDATE futuro em `colecoes` já arquiva a versão anterior automaticamente, sem precisar tocar em cada action. Corrigidos 2 problemas no desenho de exemplo da própria story: `change_summary` só olhava chaves de `OLD.payload` (perdia campos novos só em `NEW`), e a autoria da revision usava `auth.uid()` de quem _substitui_ a versão em vez de quem a escreveu — corrigido adicionando `colecoes.updated_by` (coluna nova). **Limitação importante:** o time usa Supabase cloud + Vercel, não Docker local, então as migrations 0007/0008 e o trigger nunca rodaram contra um Postgres real nesta sessão — só revisão manual linha a linha contra os padrões já usados no projeto. Task separada aberta pra aplicar via SQL Editor do Supabase e validar de verdade. Status: 🟡 Implementada, gate CONCERNS até essa verificação (05/09/2026). Gates que rodam sem Postgres: typecheck ✅, lint ✅, build ✅, test ✅ (159/159), e2e ✅ (mesmo escopo limitado).
- [Story 1.7 — Visualização consolidada do período](epics/epic-1-collections/1.7.story.md) — `/clientes/[slug]/periodo/[id]` com 4 cards (Formulário/Uploads/Texto/Áudio), status badge (usa os tokens semânticos do tema — `warning`/`info`/`success` — em vez das cores hardcoded do exemplo da story), botão "Marcar como pronto" com modal de confirmação (reaproveitou o componente `Sheet` já existente em vez de criar um Dialog novo). Achado que virou entregável extra: o breadcrumb "‹ nome do cliente" de TODAS as páginas de coleta desde a Story 1.3 apontava pra `/clientes/[slug]`, uma rota que nunca existiu (404) — construída aqui usando a action `listPeriods` (Story 1.2), que já existia pronta e nunca tinha sido consumida por nenhuma UI. Achado de infra à parte: `.claude/launch.json` apontava pra um `npx serve` estático servindo a raiz do projeto (sobra de uma versão pré-Next.js), então o preview/dev-server nunca tinha rodado a aplicação de verdade em nenhuma sessão — corrigido pra `npm run dev`. Reaproveitado `FileList` (Story 1.4) pra lista de evidências em vez de reescrever — por isso "filtro por tipo" e "ordenação por data" da AC-4 ficaram de fora (não existem em `FileList`). Primeiros testes de componente React do projeto (Testing Library + jsdom). Status: ✅ Concluída (05/09/2026). Gates: typecheck ✅, lint ✅, build ✅, test ✅ (169/169 — 10 novos), e2e ✅ (10 passando, mesmo escopo limitado).
- [Story 1.8 — Testes + gates finais do épico](epics/epic-1-collections/1.8.story.md) — **Achado crítico**: `npm run format:check` (parte do gate `quality-gates` do CI) estava falhando em 95 arquivos, incluindo dezenas nunca tocados por nenhuma story deste épico — o CI provavelmente vinha falhando silenciosamente há tempo (o hook `lint-staged` só formata arquivos staged num commit, não pega arquivos criados por outros meios). Corrigido rodando `prettier --write` em tudo. Montada toda a infraestrutura de e2e real (`db/seed-test-data.sql` idempotente que acha o usuário pelo e-mail, `tests/e2e/helpers/auth.ts`, os 5 cenários da AC-2 em `collections-flow.spec.ts`, guia `docs/testing/e2e-setup.md`) — mas não posso criar a conta de teste nem manipular a senha dela, então esses testes ficam `skipped` até a Nayara criar o usuário manualmente (criar contas é proibido pra mim mesmo com autorização explícita). Criado `docs/DATA-MODEL.md` do zero (não existia, só uma pasta vazia) e `src/components/collections/README.md`. Duas ACs ficaram genuinamente não atingidas e documentadas como tal (não maquiadas): "cada server action com teste de happy-path/erro" (a convenção real do projeto testa só schemas Zod, sem mockar Supabase, desde a Story 1.3) e Firefox no e2e (nunca instalado neste ambiente, e o próprio CI só instala Chromium — não é regressão desta story). Status: 🟡 Feito tudo que dá sem infra externa (05/09/2026). Gates: typecheck ✅, lint ✅, format ✅, build ✅, test ✅ (177/177), e2e ✅ no escopo que roda (chromium, sem o setup pendente).
